/**
 * DEPRECATED — Embeddings are now generated inline in scripts/daily-autopsy.ts (Stage 5).
 * This file is kept for reference only and will be removed in a future cleanup.
 * 
 * The new daily-autopsy.ts pipeline:
 *   Stage 1: Core metadata
 *   Stage 2: Enriched data  
 *   Stage 3: Narrative content
 *   Stage 4: Logo acquisition
 *   Stage 5: Embedding generation ← inline here
 */

console.log('⚠️  This script is deprecated.');
console.log('   Embeddings are now generated automatically as Stage 5 of scripts/daily-autopsy.ts');
console.log('   Run `npx tsx scripts/daily-autopsy.ts` instead.');
process.exit(0);
